## sync-courses

Sync English language course materials from MOOCFetcher server to local filesystem.

### Compiling for Windows (64-bit)
```
env GOOS=windows GOARCH=amd64 go build
```

### Running

This tool uses the AWS Commandline tools to do most of the heavy lifting. So, make sure you have the AWS Commandline tools installed and configured with the right credentials. [See instructions](http://docs.aws.amazon.com/cli/latest/userguide/installing.html).


You can check the tool before running
```
sync-courses -check
```

The command below will sync all the English language courses from MOOCFetcher server to a path `./archive` on your local computer.

```
sync-courses -path=./archive
```

